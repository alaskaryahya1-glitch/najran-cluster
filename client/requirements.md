## Packages
framer-motion | For smooth page transitions and hover effects
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
- Tailwind Config needs to support RTL direction if not already enabled.
- Using Google Fonts: 'Cairo' for Arabic/Headlines and 'Inter' for English body text.
- API is read-only for services based on schema provided (no create/update UI requested, but hook support will be added just in case).
